# npm 网络健壮性（Zeabur 构建机偶发 ECONNRESET）
# 见 2026-08-28 构建失败：npm ci network aborted —— 间歇性 registry 连接重置。
# 2026-08-28 追加：构建曾卡在 Docker 层镜像拉取（平台侧），注释更新以触发重建。
# 加大重试与超时，避免一次抖动导致整个构建失败。
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=300000
# 保持官方 registry（不换镜像，避免 integrity/区域差异引入新变量）
registry=https://registry.npmjs.org/