node_modules/
dist/
.env
*.log
.DS_Store
jiti/
feishu_write.json
openclaw-git-backup
secrets/
*.secret
.tiangong-env

# Runner-scoped New API secrets (never commit)
scripts/openclaw-connector/.env.local

# Build generated files
api/commit.ts

# Runtime artifacts (never commit)
openclaw-0/
openclaw_security_alerts.json
node-compile-cache/
tiangong-git-backup/
backups/

# Local planning docs (never commit)
PLAN*.md

# SQLite 数据库文件（data/tiangong.db 由 volume 持久化，不入 git）
data/
